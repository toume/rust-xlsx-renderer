# Changelog

All notable changes to `rust-xlsx-renderer` should be documented in this file.

## 0.1.0

- Initial Rust + napi-rs XLSX renderer.
- Unified `renderWorkbook(...)` API with `buffer` and `file` targets.
- Theme-based style injection.
- Structured native error codes.
- Rust and TypeScript test coverage.
